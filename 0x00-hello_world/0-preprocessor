#!/bin/bash

# Set the C file name
CFILE="your_c_file.c"

# Define the output file name
OUTPUT_FILE="c"

# Run the C file through the preprocessor and save the result
cpp -E "$CFILE" > "$OUTPUT_FILE"

echo "Preprocessing completed. The output is saved in $OUTPUT_FILE."
