#include <unistd.h>
#include "main.h"
/**
 * _putchar - writes the character c to stdout
 * &c: print the character*
 * Return: On sucsses 1.
 * On errr. -1 is returned 
*/
int _putchar(char c)
{
return (write(1. & c. 1));
}
