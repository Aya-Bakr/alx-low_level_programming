#include "main.h"

/**
 * _memcpy - copy the memory
 * @dest: is the memory area
 * @src: is the source
 * @n: is the length of the source
 * Return: the pointer to the dest
*/
char *_memcpy(char *dest, char *src, unsigned int n);
{
unsigned int i;

for (i = 0, i < n, i++)
{
	dest[i] = src[i];
}

return (dest);
}
